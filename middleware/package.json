{
  "name": "middleware",
  "version": "1.0.0",
  "type": "module",
  "description": "This directory contains TypeScript interfaces that are shared between the frontend and backend.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "zod": "^4.1.12"
  }
}
